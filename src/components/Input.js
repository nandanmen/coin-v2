import styled from 'styled-components';

const Input = styled.input`
  width: 100%;
  font-size: 1em;
  font-family: inherit;
  appearance: none;
  border-radius: 0;
  border: 0;
  &:focus {
    outline: none;
  }
`;

export default Input;
